trigger:
  batch: true
  branches:
    include:
      - "*"

variables:
- name: SolutionBaseName
  value: __SolutionBaseName__
- name: DACPACSolutionBaseName
  value: SFA.DAS.APIMDeveloper.Database
- name: BuildConfiguration
  value: release
- name: BuildPlatform
  value: any cpu

resources:
  repositories:
  - repository: self
  - repository: das-platform-building-blocks
    type: github
    name: SkillsFundingAgency/das-platform-building-blocks
    ref: refs/tags/0.3.31
    endpoint: SkillsFundingAgency

stages:
- stage: Build
  jobs:
  # - template: pipeline-templates/job/code-build.yml
  #   parameters:
  #     SolutionBaseName: $(SolutionBaseName)
  #     BuildConfiguration: $(BuildConfiguration)
  - template: azure-pipelines-templates/build/job/dacpac-template.yml@das-platform-building-blocks
    parameters:
      SolutionBaseName: $(DACPACSolutionBaseName)
      SqlProjectPath: src/SFA.DAS.APIMDeveloper.Database/SFA.DAS.APIMDeveloper.Database.sqlproj
